# AuthorTrace

**Гібридна нейромережева система детекції штучно згенерованих текстів**

AuthorTrace визначає ймовірність того, що текст згенеровано великою мовною
моделлю, поєднуючи три взаємодоповнювальні групи ознак у єдиній нейромережі
фузії. На відміну від типових детекторів, система пояснює власне рішення та
підтримує посегментну (часткову) детекцію.

---

## Ключові особливості

* **Гібридна архітектура з фузією ознак.** Один трансформер (`distilgpt2`)
  постачає одразу дві групи сигналів — перплексійні та семантичні; третя
  група — інтерпретовані стилометричні ознаки.
* **Проміжна фузія.** Кожна група ознак обробляється власним підкодувальником,
  після чого приховані представлення об'єднуються у спільному класифікаторі.
* **Пояснюваність.** Для кожного тексту обчислюється внесок кожної з трьох
  груп ознак у підсумковий вердикт (метод абляції гілок мережі).
* **Посегментна детекція.** Текст аналізується по фрагментах, що дозволяє
  локалізувати ділянки, найімовірніше згенеровані штучно.
* **Дослідження стійкості.** Передбачено оцінювання опірності детектора до
  спроб обходу шляхом «олюднення» згенерованого тексту.
* **Легкість.** Уся система працює на CPU без графічного прискорювача.

---

## Архітектура

```
            ┌─────────────────────────────────────────────┐
   текст ──▶│  Вилучення ознак                             │
            │   • стилометричні (15 ознак)                 │
            │   • перплексійні  (8 ознак, distilgpt2)      │
            │   • семантичні    (768, прихований стан LM)  │
            └───────────────┬─────────────────────────────┘
                            ▼
            ┌─────────────────────────────────────────────┐
            │  Гібридна нейромережа фузії (FusionMLP)      │
            │   стилометрія ─▶ підкодувальник ─┐           │
            │   перплексія  ─▶ підкодувальник ─┼─▶ фузія ─▶│─▶ P(штучний)
            │   семантика   ─▶ підкодувальник ─┘           │
            └─────────────────────────────────────────────┘
```

---

## Установлення

```bash
git clone <repository-url>
cd authortrace
pip install -r requirements.txt
```

Мінімальні вимоги: Python 3.10+, близько 2 ГБ вільного місця для моделей,
доступ до мережі для першого завантаження `distilgpt2` та корпусу HC3.

---

## Використання

### Наскрізний конвеєр

Конвеєр поділено на етапи, що дозволяє відновлювати роботу після переривання:

```bash
python scripts/s1_prepare.py      # 1. формування набору даних (HC3)
python scripts/s2_extract.py      # 2. вилучення ознак (виконати кілька разів)
python scripts/s3_train.py        # 3. навчання, оцінювання, графіки
python scripts/s4_robustness.py   # 4. дослідження стійкості до обходу
```

Скрипт `s2_extract.py` обробляє за один запуск частину текстів і зберігає
прогрес; його слід викликати повторно до повідомлення «ЗАВЕРШЕНО».

### Аналіз окремого тексту

```bash
python scripts/demo_cli.py --text "Текст для перевірки..."
python scripts/demo_cli.py --file document.txt
python scripts/demo_cli.py --file ukrainian.txt --auto-translate
```

Прапорець `--auto-translate` вмикає автоматичний переклад
україномовного тексту англійською перед аналізом.

### Інтерактивний веб-інтерфейс

```bash
streamlit run app/streamlit_app.py
```

Веб-інтерфейс надає:

* **Галерею тестових прикладів** (11 курованих текстів у трьох категоріях —
  «Людина», «Штучний текст», крайові випадки). Кожен приклад
  супроводжується поясненням, чому він є показовим.
* **Аналіз довільного тексту** з імовірністю штучного походження.
* **Підтримку україномовних текстів** через автоматичний переклад
  англійською (вмикається у бічній панелі).
* **Візуалізацію внеску** кожної з трьох груп ознак у вердикт.
* **Посегментний аналіз** з підсвічуванням підозрілих фрагментів.
* **Регульований поріг класифікації** у бічній панелі.

### Програмний інтерфейс

```python
from src.detector import AuthorTraceDetector

detector = AuthorTraceDetector(
    model_path="results/authortrace_model.pt",
    scaler_path="results/scaler.json",
)

# Англомовний текст — пряма обробка
result = detector.analyze("English text for analysis...")
print(result["verdict"], result["ai_probability"])

# Україномовний текст — з перекладом
result = detector.analyze("Український текст...", translate=True)
print(result["verdict"], result["ai_probability"])
print(result["translation"])      # деталі: мова оригіналу, переклад
```

---

## Підтримка української мови

Базова модель навчена на англомовному корпусі HC3. Для україномовних
текстів передбачено опційний прозорий переклад через бібліотеку
`deep-translator` (безкоштовний публічний бекенд Google Translate, не
потребує ключа API). У `streamlit`-інтерфейсі він керується перемикачем
у бічній панелі, у CLI — прапорцем `--auto-translate`, у програмному
інтерфейсі — параметром `translate=True`.

Переклад виконується автоматично лише для текстів із переважно
кириличним складом; англомовний вхід проходить без змін. Довгі тексти
розбиваються на фрагменти за межами речень, щоб не перевищувати ліміт
ендпойнта (~5000 символів на запит).

---

## Обробка довгих текстів

Контекст `distilgpt2` обмежено 1024 токенами, а робоче вікно — 220
токенами (для швидкості на CPU). Для текстів, що перевищують цей розмір,
вилучення перплексійних і семантичних ознак виконується покроково:
текст ділиться на послідовні непересічні фрагменти, токен-рівневі
лог-імовірності та ранги конкатенуються, а семантичний ембединг
усереднюється за всіма фрагментами. Усі агреговані метрики (середня
лог-імовірність, перплексія, burstiness, частки top-1/top-k) залишаються
коректними статистиками над повним текстом, тож довгі документи більше
не зводяться до перших ~150 слів.

Посегментний аналіз для довгих текстів адаптивно укрупнює сегменти,
щоб їхня загальна кількість не перевищувала розумної межі (40).

---

## Усунення несправностей

**`ModuleNotFoundError: No module named 'torchvision'`** — у нових
версіях `transformers` Auto-класи ліниво підвантажують модулі обробки
зображень. Кодова база використовує прямі класи `GPT2LMHeadModel` /
`GPT2TokenizerFast` й обходить цей шлях; додатково
`requirements.txt` фіксує `transformers<4.50`. Якщо помилка все ж
з'являється — встановіть `pip install torchvision` як швидкий
обхідний шлях.

---

## Структура репозиторію

```
authortrace/
├── config.yaml              # параметри системи
├── requirements.txt
├── src/
│   ├── data_loader.py       # формування набору даних (HC3)
│   ├── features/
│   │   ├── stylometric.py   # лінгвостатистичні ознаки
│   │   ├── perplexity.py    # перплексійні ознаки
│   │   └── __init__.py      # оркестратор вилучення ознак
│   ├── model.py             # нейромережа фузії FusionMLP
│   ├── train.py             # цикл навчання
│   ├── evaluate.py          # метрики та побудова графіків
│   ├── explain.py           # інтерпретація та посегментний аналіз
│   ├── robustness.py        # перетворення для перевірки стійкості
│   ├── translate.py         # переклад україномовних текстів
│   └── detector.py          # високорівневий інтерфейс
├── scripts/                 # етапи конвеєра, демонстрація
├── app/
│   ├── streamlit_app.py     # інтерактивний веб-інтерфейс
│   └── examples.json        # бібліотека тестових прикладів
├── tests/                   # модульні тести
└── results/                 # моделі, метрики, ілюстрації
```

---

## Набір даних

Використовується корпус **HC3 (Human ChatGPT Comparison Corpus)** —
відкритий набір пар відповідей на однакові запитання, написаних людиною та
згенерованих моделлю ChatGPT. Корпус завантажується автоматично з
HuggingFace Hub.

---

## Результати

Експериментальні результати (точність, F1, AUC, абляційне дослідження,
порівняння з базовими методами, оцінювання стійкості) зберігаються у файлах
`results/metrics.json` та `results/robustness.json`, ілюстрації — у
каталозі `results/`.

---

## Ліцензія

MIT License — див. файл `LICENSE`.
