# --- Базові залежності ---
torch>=2.2
# Верхня межа на transformers потрібна тому, що з 4.50+ деякі шляхи
# імпорту (зокрема через Auto-класи) починають тягнути torchvision.
# Ми все одно перейшли на прямі імпорти GPT2*, але збереження діапазону
# додатково страхує від несподіванок при оновленні бібліотеки.
transformers>=4.40,<4.50
datasets>=2.18
huggingface_hub>=0.23
numpy>=1.24
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0
streamlit>=1.30

# --- Переклад україномовних текстів на англійську ---
# Безкоштовний публічний бекенд Google Translate, не потребує API-ключа.
deep-translator>=1.11
